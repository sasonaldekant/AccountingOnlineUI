{
  "name": "@dyn-ui/react",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "vite build",
    "prepare-dist-pkg": "node scripts/generate-dist-package.js",
    "prepublishOnly": "pnpm run prepare-dist-pkg && pnpm run build",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:box": "vitest --environment=jsdom src/components/DynBox"
  },
  "dependencies": {
    "classnames": "^2.5.1"
  },
  "devDependencies": {
    "@types/classnames": "^2.3.0"
  }
}
